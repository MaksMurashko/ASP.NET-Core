@model UniqueProducts.Models.Material

@{
    ViewData["Title"] = "Details";
}

<h1>Подробности</h1>

<div>
    <h4>Материал</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.MaterialName)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.MaterialName)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.MaterialDescript)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.MaterialDescript)
        </dd>
    </dl>
</div>
<div>
    @if (User.Identity.IsAuthenticated && (User.IsInRole("SuperAdmin") || User.IsInRole("Admin")))
    {
        <span>  <a asp-action="Edit" asp-route-id="@Model?.MaterialId">Изменить</a> |</span>
    }
    <a asp-action="Index">Назад</a>
</div>
